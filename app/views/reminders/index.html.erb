<div class="container">
  <div class="row">
    <div class="col">
      <h1 class="pb-2">Reminders</h1>
      <%= button_to "Add Reminder", new_reminder_path, method: :get, class: "btn btn-primary"%>
      <div class="table-container p-2">
        <table class="table">
          <thead>
          <tr>
            <th scope="col">Reminder Date</th>
            <th scope="col">Reminder</th>
            <th scope="col">Edit</th>
            <th scope="col">Delete</th>
          </tr>
        </thead>
        <tbody>
        <% @reminders.each do |reminder| %>
          <tr>
            <td><%= reminder.reminder_date %> </td>
            <td><%= reminder.body %> </td>
            <td>
              <%= button_to "Edit", edit_reminder_path(reminder), method: :get, class: "btn btn-primary" %>
            </td>
            <td>
              <%= button_to "Delete", reminder_path(reminder), method: :delete, class: "btn btn-danger" %>
            </td>
          </tr>
        <% end %>
        </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
